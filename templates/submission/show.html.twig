{% extends 'base.html.twig' %}

{% block title %}Submission{% endblock %}

{% block body %}
    <h1>Submission Details</h1>
    <hr>
    <h2>Student: <a
                href="{{ path('app_student_show', {'id': submission.student.id}) }}">{{ submission.student.name }}</a>
    </h2>
    <br>
    <h4>For Course: <a
                href="{{ path('app_course_show', {'id': submission.coursework.course.id}) }}">{{ submission.coursework.course.name }}</a>
    </h4>
    <h4>For Coursework: <a
                href="{{ path('app_course_work_show', {'id': submission.coursework.id}) }}">{{ submission.coursework.name }}</a>
    </h4>
    <br>
    <p>Hand in Date: {{ submission.handInDate ? submission.handInDate|date('d/m/Y') : '' }}</p>
    <p>Second Submission? {{ submission.resubmitted ? 'Yes' : 'No' }}</p>
    <p>Mark: {{ submission.mark }}%</p>
    <p>Grade: {{ submission.grade }}</p>
    <br>
    <div class="btn-group" role="group">
        <a class="btn btn-primary" href="{{ path('app_submission_edit', {'id': submission.id}) }}">Edit</a>
        {{ include('submission/_delete_form.html.twig') }}
    </div>
    <br>
    <br>
    <a class="btn btn-secondary" href="{{ path('app_submission_index') }}">Back to all Submissions</a>
    <hr>
{% endblock %}
