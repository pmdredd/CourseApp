<form method="post" action="{{ path('app_course_work_delete', {'id': course_work.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course_work.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
